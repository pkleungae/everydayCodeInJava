For init the k-centroid, i used random method to do it.That means the array of centroid would be different. 



there are methods in my program

Classifier(): for calulating the distance between each data point and centroid, i used Euclidean distance to do it. 

initCentroid(): for init the centroid, i used random method to do it.That means the array of centroid would be different. 

init_cluster_of_each_dataPoint(): for init cluster. storing the corespoind cluster of each data point.

initDateSet():init data set

printResult():Print out result.
